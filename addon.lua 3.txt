-- LMT__V1 Script (Updated with Tab2 Por Hub + Tab4 Muốn thêm script j ib + copy functions)
print("LMT__V1 - Đang khởi động...")

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Tạo ScreenGui chính
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LMTMenuGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

-- Intro Text "LMT__V1"
local IntroLabel = Instance.new("TextLabel")
IntroLabel.Size = UDim2.new(0.6, 0, 0.15, 0)
IntroLabel.Position = UDim2.new(0.2, 0, 0.425, 0)
IntroLabel.BackgroundTransparency = 1
IntroLabel.Text = "LMT__V1"
IntroLabel.TextColor3 = Color3.fromRGB(100, 180, 255)
IntroLabel.TextScaled = true
IntroLabel.Font = Enum.Font.SourceSansBold
IntroLabel.TextStrokeTransparency = 0.7
IntroLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
IntroLabel.Parent = ScreenGui

wait(2.5)
local fadeTween = TweenService:Create(IntroLabel, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In), { 
    TextTransparency = 1, 
    TextStrokeTransparency = 1 
})
fadeTween:Play()
fadeTween.Completed:Connect(function()
    IntroLabel:Destroy()
    
    spawn(function()
        -- Main Flag Frame
        local MainFrame = Instance.new("Frame")
        MainFrame.Size = UDim2.new(0, 40, 0, 36)
        MainFrame.Position = UDim2.new(0.08, 0, 0.4, 0)
        MainFrame.BackgroundTransparency = 1
        MainFrame.Parent = ScreenGui

        local FlagImage = Instance.new("ImageButton")
        FlagImage.Size = UDim2.new(1, 0, 1, 0)
        FlagImage.Image = "rbxassetid://11548089657" -- Cờ VN
        FlagImage.BackgroundTransparency = 1
        FlagImage.Parent = MainFrame

        -- Inner Menu Frame (tăng cao hơn để chứa Tab4)
        local InnerFrame = Instance.new("Frame")
        InnerFrame.Size = UDim2.new(0, 200, 0, 180)  -- Tăng chiều cao
        InnerFrame.Position = UDim2.new(0.5, -100, 0.5, -90)
        InnerFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        InnerFrame.BorderSizePixel = 2
        InnerFrame.BorderColor3 = Color3.fromRGB(80, 80, 255)
        InnerFrame.Visible = false
        InnerFrame.Parent = ScreenGui

        -- Draggable cho lá cờ và inner frame (giữ nguyên như cũ, tôi skip code dài để ngắn gọn, copy từ trước)

        -- ... (Paste phần draggable cho FlagImage và InnerFrame từ code cũ ở đây)

        FlagImage.MouseButton1Click:Connect(function()
            InnerFrame.Visible = not InnerFrame.Visible
        end)

        -- CDVN Button
        local CDVNButton = Instance.new("TextButton")
        CDVNButton.Size = UDim2.new(0.45, 0, 0.15, 0)
        CDVNButton.Position = UDim2.new(0.05, 0, 0.05, 0)
        CDVNButton.Text = "CDVN"
        CDVNButton.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
        CDVNButton.TextColor3 = Color3.fromRGB(200, 220, 255)
        CDVNButton.Font = Enum.Font.SourceSansBold
        CDVNButton.TextSize = 18
        CDVNButton.Parent = InnerFrame

        -- Fix Lag Button
        local FixLagButton = Instance.new("TextButton")
        FixLagButton.Size = UDim2.new(0.45, 0, 0.15, 0)
        FixLagButton.Position = UDim2.new(0.05, 0, 0.23, 0)
        FixLagButton.Text = "Fix Lag"
        FixLagButton.BackgroundColor3 = Color3.fromRGB(60, 40, 40)
        FixLagButton.TextColor3 = Color3.fromRGB(255, 200, 200)
        FixLagButton.Font = Enum.Font.SourceSansBold
        FixLagButton.TextSize = 16
        FixLagButton.Parent = InnerFrame

        -- Tab3: Sóng Thần Brainrot
        local Tab3Button = Instance.new("TextButton")
        Tab3Button.Size = UDim2.new(0.45, 0, 0.15, 0)
        Tab3Button.Position = UDim2.new(0.05, 0, 0.41, 0)
        Tab3Button.Text = "Sóng Thần Brainrot"
        Tab3Button.BackgroundColor3 = Color3.fromRGB(80, 40, 100)
        Tab3Button.TextColor3 = Color3.fromRGB(220, 180, 255)
        Tab3Button.Font = Enum.Font.SourceSansBold
        Tab3Button.TextSize = 14
        Tab3Button.Parent = InnerFrame

        -- Tab4 mới: Muốn thêm script j ib (dưới Tab3)
        local Tab4Button = Instance.new("TextButton")
        Tab4Button.Size = UDim2.new(0.45, 0, 0.15, 0)
        Tab4Button.Position = UDim2.new(0.05, 0, 0.59, 0)
        Tab4Button.Text = "Muốn thêm script j ib"
        Tab4Button.BackgroundColor3 = Color3.fromRGB(100, 60, 30)
        Tab4Button.TextColor3 = Color3.fromRGB(255, 220, 180)
        Tab4Button.Font = Enum.Font.SourceSansBold
        Tab4Button.TextSize = 13
        Tab4Button.Parent = InnerFrame

        -- (Giữ TabsFrame cho CDVN và FixTabsFrame như cũ, skip dài dòng)

        -- AJJ Frame (bên phải khi bấm Tab3)
        local AJJFrame = Instance.new("Frame")
        AJJFrame.Size = UDim2.new(0, 180, 0, 120)
        AJJFrame.Position = UDim2.new(1.05, 0, 0, 0)
        AJJFrame.BackgroundColor3 = Color3.fromRGB(40, 20, 60)
        AJJFrame.BorderSizePixel = 2
        AJJFrame.BorderColor3 = Color3.fromRGB(150, 80, 200)
        AJJFrame.Visible = false
        AJJFrame.Parent = InnerFrame

        local AJJButton = Instance.new("TextButton")
        AJJButton.Size = UDim2.new(0.9, 0, 0.4, 0)
        AJJButton.Position = UDim2.new(0.05, 0, 0.3, 0)
        AJJButton.Text = "AJJ Hub"
        AJJButton.BackgroundColor3 = Color3.fromRGB(90, 50, 120)
        AJJButton.TextColor3 = Color3.fromRGB(240, 200, 255)
        AJJButton.Font = Enum.Font.SourceSansBold
        AJJButton.TextSize = 20
        AJJButton.Parent = AJJFrame

        AJJButton.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/832af29a13a41951e31129a974a1e8cc.lua"))()
            print("AJJ Hub loaded!")
        end)

        -- Frame mới cho Tab2 Por Hub (dưới AJJ Hub trong AJJFrame)
        local PorFrame = Instance.new("Frame")
        PorFrame.Size = UDim2.new(0.9, 0, 0.4, 0)
        PorFrame.Position = UDim2.new(0.05, 0, 0.75, 0)  -- Dưới AJJ Hub
        PorFrame.BackgroundTransparency = 1
        PorFrame.Parent = AJJFrame

        local PorButton = Instance.new("TextButton")
        PorButton.Size = UDim2.new(1, 0, 1, 0)
        PorButton.Text = "Por Hub"
        PorButton.BackgroundColor3 = Color3.fromRGB(120, 30, 60)
        PorButton.TextColor3 = Color3.fromRGB(255, 180, 200)
        PorButton.Font = Enum.Font.SourceSansBold
        PorButton.TextSize = 18
        PorButton.Parent = PorFrame

        PorButton.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://pastefy.app/12lE27wY/raw", true))()
            print("Por Hub loaded!")
        end)

        -- Frame mới cho Tab4 (bên phải khi bấm Tab4)
        local Tab4Frame = Instance.new("Frame")
        Tab4Frame.Size = UDim2.new(0, 180, 0, 140)
        Tab4Frame.Position = UDim2.new(1.05, 0, 0, 0)
        Tab4Frame.BackgroundColor3 = Color3.fromRGB(60, 40, 20)
        Tab4Frame.BorderSizePixel = 2
        Tab4Frame.BorderColor3 = Color3.fromRGB(200, 120, 60)
        Tab4Frame.Visible = false
        Tab4Frame.Parent = InnerFrame

        -- TikTok button (copy "dcmoilha")
        local TikTokButton = Instance.new("TextButton")
        TikTokButton.Size = UDim2.new(0.9, 0, 0.4, 0)
        TikTokButton.Position = UDim2.new(0.05, 0, 0.1, 0)
        TikTokButton.Text = "TikTok"
        TikTokButton.BackgroundColor3 = Color3.fromRGB(0, 150, 200)
        TikTokButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        TikTokButton.Font = Enum.Font.SourceSansBold
        TikTokButton.TextSize = 18
        TikTokButton.Parent = Tab4Frame

        TikTokButton.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard("dcmoilha")
                print("Đã copy: dcmoilha")
            else
                print("Executor không hỗ trợ setclipboard!")
            end
        end)

        -- Facebook button (copy link)
        local FacebookButton = Instance.new("TextButton")
        FacebookButton.Size = UDim2.new(0.9, 0, 0.4, 0)
        FacebookButton.Position = UDim2.new(0.05, 0, 0.55, 0)
        FacebookButton.Text = "Facebook"
        FacebookButton.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
        FacebookButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        FacebookButton.Font = Enum.Font.SourceSansBold
        FacebookButton.TextSize = 18
        FacebookButton.Parent = Tab4Frame

        FacebookButton.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard("https://www.facebook.com/share/18QphEcU9g/?mibextid=wwXIfr")
                print("Đã copy link Facebook!")
            else
                print("Executor không hỗ trợ setclipboard!")
            end
        end)

        -- Toggle logic (thêm cho Tab4)
        CDVNButton.MouseButton1Click:Connect(function()
            -- ... (giữ nguyên toggle cho CDVN, Fix, ẩn các frame khác)
            AJJFrame.Visible = false
            Tab4Frame.Visible = false
        end)

        FixLagButton.MouseButton1Click:Connect(function()
            -- ... (tương tự)
            AJJFrame.Visible = false
            Tab4Frame.Visible = false
        end)

        Tab3Button.MouseButton1Click:Connect(function()
            AJJFrame.Visible = not AJJFrame.Visible
            Tab4Frame.Visible = false
            -- Ẩn CDVN/Fix nếu cần
        end)

        Tab4Button.MouseButton1Click:Connect(function()
            Tab4Frame.Visible = not Tab4Frame.Visible
            AJJFrame.Visible = false
            -- Ẩn CDVN/Fix nếu cần
        end)

        print("LMT__V1 - Đã thêm Por Hub & Tab4 copy TikTok/Facebook!")
    end)
end)
